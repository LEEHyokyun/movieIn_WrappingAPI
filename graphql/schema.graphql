type Movie {
    id : Int!
    title : String!
    rating : Float!
    summary : String!
    language : String!
    medium_cover_image : String!
}

type Query {
    seemovie(id: Int) : [Movie]!
    checkmovie : [Movie]!
    getmovie(limit: Int, rating: Float) : [Movie]!
}